# App Configuration
STORAGE_MODE=local
MAX_UPLOAD_MB=5

# Development Settings
NODE_ENV=development

# Admin Credentials (optional - can be changed in lib/auth.ts)
# These are the default credentials for testing
# ADMIN_EMAIL=admin@memoriae.com
# ADMIN_PASSWORD=admin123
